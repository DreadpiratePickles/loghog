"""Stage 07: the dataset, as drafts, and the one gate that adopts one.

Everything upstream is a proposal. A golden case is an acceptance criterion —
the thing every later change is measured against — so nothing here writes into a
goldens file except `promote`, and `promote` refuses without a name.
"""
